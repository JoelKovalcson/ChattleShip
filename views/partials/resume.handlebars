
<div class="flex flex-col w-80">
    <h3>Resume Game</h3>
    <a>
        <ul class="flex flex-col  border-box">
			{{#each resumeList}}
			<li>
				<div class="flex flex-col text-center justify-center p-6 rounded-lg shadow-lg bg-white max-w-full">
					<h4 class="text-black text-xl font-semibold">Game: {{this.gameId}}</h4>
					<h5 class="text-black text-l font-medium">{{this.player1}} vs. {{this.player2}}</h5>
					<p class="text-black text-base font-medium mb-2">Turn: {{this.playerTurn}}</p>
					<a href="/game/{{this.gameId}}"><button type="button" class="w-full border-2 border-transparent rounded-xl outline hover:shadow-xl hover:brightness-200 hover:bg-sky-900  hover:shadow-black active:bg-sky-800">Resume Game</button></a>
				</div>
			</li>
			{{/each}}
        </ul>
    </a>   
</div>